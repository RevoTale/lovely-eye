version: "3"

vars:
  GO: go
  TSC: pnpm --dir ../../dashboard exec tsc

tasks:
  build:
    desc: Build tracker artifact
    cmds:
      - "{{.GO}} run build.go"

  typecheck:
    desc: Typecheck tracker TypeScript sources
    cmds:
      - "{{.TSC}} -p ../server/static/tsconfig.json"
