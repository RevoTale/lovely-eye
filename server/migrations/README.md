# Migrations
This module is compiled with the independend binary in `cmd` directory and handle only the database automatic migrations feature.
So, that way binary size of the main server is not increasing over time. Doesn't matter how much changes were applied.