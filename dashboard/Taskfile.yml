version: "3"

tasks:
  test:
    desc: Run dashboard Biome and TypeScript checks
    cmds:
      - bun run test

  generate:
    desc: Generate dashboard GraphQL artifacts
    cmds:
      - bun run codegen
