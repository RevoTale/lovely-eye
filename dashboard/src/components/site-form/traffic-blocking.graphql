query GeoIPCountries($search: String, $paging: PagingInput!) {
  geoIPCountries(search: $search, paging: $paging) {
    ...TrafficBlockingCountryFields
  }
}

fragment TrafficBlockingCountryFields on GeoIPCountry {
  code
  name
}
