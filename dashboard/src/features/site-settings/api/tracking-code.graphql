mutation RegenerateSiteKey($id: ID!) {
  regenerateSiteKey(id: $id) {
    ...TrackingCodeSiteSummaryFields
  }
}

fragment TrackingCodeSiteSummaryFields on Site {
  id
  domains
  name
  publicKey
  createdAt
}
