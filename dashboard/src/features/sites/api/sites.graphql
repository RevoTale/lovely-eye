query Sites($paging: PagingInput!) {
  sites(paging: $paging) {
    ...SiteSummaryFields
  }
}

mutation CreateSite($input: CreateSiteInput!) {
  createSite(input: $input) {
    ...SiteSummaryFields
  }
}

fragment SiteSummaryFields on Site {
  id
  domains
  name
  publicKey
  createdAt
}
